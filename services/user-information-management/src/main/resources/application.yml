spring:
  application:
    name: user-information-management
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/priceCommunicationSystem?characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8040/eureka/
server:
  port: 8010